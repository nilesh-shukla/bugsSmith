services:
  - type: web
    name: bugssmith
    env: node
    plan: free
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO
    branch: main
    buildCommand: npm install && cd frontend && npm install && npm run build
    startCommand: npm start
    root: bugsSmith-b || . # leave blank or set to backend folder if needed